spring:
  application:
    name: microservice-feign-server
server:
  port: 8763
eureka:
  client:
    serviceUrl:
      defaultZone: http://${security.user.name}:${security.user.password}@localhost:8761/eureka/

security:
  user:
    password: xiaoy
    name: xiaoy

